# Prochaines actions

## Action unique

**Capability Packs 1 — verticale NestJS + Next.js + React Native**

Transformer `base`, `auth`, `rbac` et `files` en composition réelle :

1. manifests Starter/Capability V2 et validation croisée ;
2. baseline `base` minimale pour les trois starters ;
3. overlays déclaratifs `auth`, `rbac`, `files` ;
4. goldens `base`, `auth`, `rbac`, `files` installés et vérifiés ;
5. refus explicite des targets et capabilities non `ready`.

## Ensuite

1. Capability Packs 2 : parité Spring + Angular + Flutter.
2. R8 : golden runtimes réels sur les deux verticales.
3. R9 : compilateur de domaine CRUD NestJS/Spring.
4. R10 : upgrades et migrations blueprint.
5. R11 : distribution CLI/packages.
6. R12 : métriques d'adoption et feedback projets dérivés.

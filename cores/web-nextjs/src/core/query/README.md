# `core/query` — cadrage (vide en Web 1)

Emplacement réservé à la **gestion d'état serveur** (TanStack Query — ADR-012) du Web Core.
**Aucun code en Web 1.**

Volontairement **absent** en Web 1 :

- TanStack Query (provider, hooks, cache) ;
- état global client (Zustand) ;
- intégration Axios / Orval (le client officiel est **Fetch**, sans Axios — voir
  `@enistere/api-client-fetch`).

> Ne rien ajouter ici sans mission dédiée.

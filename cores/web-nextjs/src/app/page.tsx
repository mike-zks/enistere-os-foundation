import type { ReactElement } from "react";
import { FoundationStatus } from "../features/foundation-status/foundation-status";

/** Page d'accueil technique (Server Component). */
export default function HomePage(): ReactElement {
  return <FoundationStatus />;
}
